{
  "context": "You are working in a development environment where most projects are written in Rust.\nThe source code is located in the 'src' directory.\nUse `cargo` commands for building and testing Rust projects.\nWhen the user asks to make a commit, remember to run `git add .` first so all the changed files will be committed."
}
